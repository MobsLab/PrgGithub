function [Acc,ConfMat] = MultiClass_SVM_SB(Data)

%% Performs one-on-one multiclassification with SVM
%% Data = structure with one field per class
%% Implements leave one out cross validation


NumClass = length(fieldnames(Data));
















end