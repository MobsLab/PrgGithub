
function NewEpoch=NoMoreOverlaps(Epoch)

% For intervalSet files

beg=Start(Epoch);
fin=Stop(Epoch);

while i<length(beg)-1
    if (fin(i)-beg(i+1))>0
        fin(i)=[];
        beg(i)=[];
    else
    i=i+1;    
    end
end

NewEpoch=intervalSet(beg,fni)
end