function C = current_dir(A)
% C = current_dir(A) returns current datasets directory 
  
% TODO % for the moment this works only for the one dataset one dir data
% layout
  
  
  
  C = A.curr_dset;
  C = [A.parent_dir filesep A.curr_dset];
  
  
  